baseURL = "https://blog.kullbach.net/"
languageCode = "en-us"
title = "kullbach.net"
theme = "minilist"

[markup]
  [markup.highlight]
    noClasses = false
    style = "monokai"
[outputs]
  home = ["HTML", "RSS"]
  section = ["HTML", "RSS"]
  taxonomy = ["HTML", "RSS"]