baseURL = "https://philipp.kullbach.net/"
languageCode = "en-us"
title = "Philipp's Blog"
theme = "minilist"

[markup]
  [markup.highlight]
    noClasses = false
    style = "monokai"
[outputs]
  home = ["HTML", "RSS"]
  section = ["HTML", "RSS"]
  taxonomy = ["HTML", "RSS"]